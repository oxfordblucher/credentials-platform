import { evtEmitter } from "./emitter.js";
import { Events } from "./event.js";

evtEmitter.on(Events.CREDENTIAL_CREATED, () => {

})

evtEmitter.on(Events.CREDENTIAL_SUBMITTED, () => {

})

evtEmitter.on(Events.CREDENTIAL_VERIFIED, () => {
  
})

evtEmitter.on(Events.CREDENTIAL_REVOKED, () => {
  
})

evtEmitter.on(Events.INVITE_CREATED, () => {
  
})

evtEmitter.on(Events.INVITE_ACCEPTED, () => {
  
})