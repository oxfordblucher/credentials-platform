import { Request, Response, NextFunction } from 'express';

export const getSessions = (req: Request, res: Response, next: NextFunction) => {

}

export const revokeSession = (req: Request, res: Response, next: NextFunction) => {

}

export const revokeOtherSessions = (req: Request, res: Response, next: NextFunction) => {
  
}

export const revokeAllSessions = (req: Request, res: Response, next: NextFunction) => {

}